---
name: pre-pr-review
description: PR作成前に4つの独立した観点から現在のブランチの差分をレビューし、根拠のある指摘を統合する。
disable-model-invocation: true
---

# PR作成前レビュー

`$ARGUMENTS` にbaseブランチやIssueへの参照が指定されていれば優先する。未指定なら追跡先、PR設定、デフォルトブランチを調べてbaseを特定する。曖昧なら候補を明示して確認する。マージベースから現在のHEADまでの差分に加え、未コミット変更があればその有無を明記して対象に含める。削除・改名・新規ファイルも確認する。

可能なら新しいClaude Codeセッションから実行する。実装者の説明は事実と仮定せず、差分、現行コード、要件、テスト、文書を一次情報として扱う。要件の所在を探し、見つからなければその限界を示す。

次の4つのsubagentを、それぞれ独立した依頼として起動する。各agentに同じbase、差分範囲、利用可能な要件の所在を渡す。他のagentの指摘や結論を事前に渡さない。並列実行が可能なら並列に行う。

1. `requirements-reviewer`
2. `codebase-consistency-reviewer`
3. `bug-hunter`
4. `test-reviewer`

リポジトリ固有の `review-history` がある場合は、該当するagentがその知識を探索仮説として利用する。各agentには差分外の呼び出し側や対になる実装まで探索し、可能な範囲で検索結果や安全な実行結果を示すよう依頼する。実行不能な場合はその理由を記録する。

全結果を受け取り、重複を統合し、現行コード・要件・既存テストで根拠を確かめる。裏付けのない指摘を除き、残る指摘を Blocking / Should fix / Non-blocking に分類する。各件に該当箇所、条件、影響、根拠、発見したagent名を書く。該当がなければ「なし」と書く。

最後に Review coverage として実施した4観点、確認できなかった情報、レビュー範囲を記す。明示的な依頼がない限りコードを修正しない。
